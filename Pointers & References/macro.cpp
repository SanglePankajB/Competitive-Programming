/*  #define Pi 3.14  ==>This is macro

 A macro is a piece of code that is replaced by the value of the macro.
 Macro is defined by #define . Whenever a macro name is encountered by the compiler, it replaces the name with the definition of the macro. 
 Macro definitions does not need ';' at end).

 */

#include<bits/stdc++.h>
using namespace std;

#define Pi 3.14         // Macro is not a variable so it does not take any memory space it just replace the value
#define frr for(int i=0;i<n;i++)

int main()
{
    cout<<Pi*0;
    int n = 3;

    frr{
        "Pankaj  ";
    }
    return 0;
}

 